<h1>Listing website_menus</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @website_menus.each do |website_menu| %>
      <tr>
        <td><%= link_to 'Show', website_menu %></td>
        <td><%= link_to 'Edit', edit_website_menu_path(website_menu) %></td>
        <td><%= link_to 'Destroy', website_menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Website menu', new_website_menu_path %>
