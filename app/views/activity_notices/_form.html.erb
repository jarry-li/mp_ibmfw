<%= form_for(@activity_notice) do |f| %>
  <% if @activity_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_notice.errors.count, "error") %> prohibited this activity_notice from being saved:</h2>

      <ul>
      <% @activity_notice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
